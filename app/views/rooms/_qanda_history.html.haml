.qanda--history
  .history--title
    あなたの相談履歴
  .history--boxes
    - if user_signed_in?
      - @historys.each do |history|
        .history__list
          = link_to room_chats_path(history), class: "qanda--history__room" do
            %span.qanda--history__room-name
              Q : #{history.name}
            %span.qanda--history__room-date
              = history.created_at.strftime("作成日：%Y年%m月%d日 %H時%M分")
            %span.qanda--history__room-message
              = history.show_last_chat