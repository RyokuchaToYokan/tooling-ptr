-# chatの表示部分
.chats
  .chat
    .upper-chat
      .upper-chat__user-name
        -# = @chat.user_id.nickname
        #{@room.chats}
      .upper-chat__date
        -# = chat.created_at.strftime("%Y年%m月%d日 %H時%M分")
    -# .lower-chat
    -#   - if chat.content.present?
    -#     %p.lower-chat__content
    -#       = chat.content
    -#   = image_tag chat.image.url, class: 'lower-chat__image' if chat.image.present?